<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.0/css/bulma.min.css">
    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
  </head>
  <body>

    <div class="navigation">
      <ul>
        <button class="button **is-large is-success is-rounded**"><a href="/students/new">Add Student</a></button>
      
        <button class="button **is-large is-success is-rounded**"><a href="/parties">View Parties</a></button>

        <button class="button **is-large is-success is-rounded**"><a href="/logout">Logout</a></button>
      </ul>
    </div>

    <h2>My Students</h2>

    <% @students.each do |student| %>
      <div>
        <%= student.student_name %> <%= student.student_birthday %><br>
        <button><a href="/students/<%= student.id %>/edit">Edit Student</a></button>
        <form method="post" action="/students/<%=student.id%>/delete">
          <input id="hidden" type="hidden" name="_method" value="DELETE">
          <input type="submit" value="Delete Student">
        </form>
        <a href="/students/<%= student.id %>/parties/new">Add A Party</a><br><br>

      </div>
    <% end %>


  </body>
</html>
